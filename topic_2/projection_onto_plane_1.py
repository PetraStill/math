"""
Якщо площина довільна, то потрібно її нормалізувати для отримання ортонормованого базису.
"""
import numpy as np

# Площина, задана двома ортогональними векторами
v1 = np.array([1, 1, 0])
v2 = np.array([-1, 1, 0])

# Нормалізуємо для отримання ортонормованого базису
u1 = v1 / np.linalg.norm(v1)
u2 = v2 / np.linalg.norm(v2)

print("Площина з ортонормованим базисом:")
print(f"  u1 = {u1}")
print(f"  u2 = {u2}")

# Проєктуємо вектор
v = np.array([3, 2, 4])
proj = (v @ u1) * u1 + (v @ u2) * u2

print(f"\nВектор v = {v}")
print(f"Проєкція на площину = {proj}")
